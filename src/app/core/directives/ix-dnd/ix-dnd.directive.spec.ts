import { IxDndDirective } from './ix-dnd.directive';

describe('IxDndDirective', () => {
  it('should create an instance', () => {
    const directive = new IxDndDirective();
    expect(directive).toBeTruthy();
  });
});
