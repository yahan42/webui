import { IxAnimateDirective } from './ix-animate.directive';

describe('IxAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new IxAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
